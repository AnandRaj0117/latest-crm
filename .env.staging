# Staging Environment Configuration
NODE_ENV=staging

# Backend Configuration (Add these to Vercel Environment Variables)
# PORT=4000 (Vercel will assign this automatically)
# MONGODB_URI=<your-staging-mongodb-uri>
# CORS_ORIGIN=<your-staging-frontend-url>
# JWT_SECRET=<your-staging-jwt-secret>
# JWT_EXPIRE=7d

# Frontend Configuration (Add these to Vercel Environment Variables)
# REACT_APP_API_URL=<your-staging-backend-url>/api
# REACT_APP_ENV=staging
